# TODO

Ongoing tasks and known gaps. Add items here rather than leaving TODOs scattered in code.

## Pending

- Define scratch org shape in `config/project-scratch-def.json` - needs target features and settings confirmed
- Add initial metadata to `force-app/main/default/` once scope is defined
- Configure CI/CD pipeline for automated deployments

## Decisions Needed

- Confirm whether project targets a scratch org, sandbox, or production flow
- Decide on namespace usage (currently empty in `sfdx-project.json`)
