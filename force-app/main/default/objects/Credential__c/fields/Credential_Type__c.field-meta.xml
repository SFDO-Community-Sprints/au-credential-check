<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Credential_Type__c</fullName>
    <deleteConstraint>Restrict</deleteConstraint>
    <description>The document category this credential belongs to. Drives the volunteer form label (Type__c), the link expiry window (Link_Expiry_Days__c), the expiry notification lead time (Days_Before_Expiry__c), and whether an expiry date is required (Does_Not_Expire__c).</description>
    <label>Credential Type</label>
    <referenceTo>Credential_Type__c</referenceTo>
    <relationshipLabel>Credentials</relationshipLabel>
    <relationshipName>Credentials</relationshipName>
    <required>true</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
