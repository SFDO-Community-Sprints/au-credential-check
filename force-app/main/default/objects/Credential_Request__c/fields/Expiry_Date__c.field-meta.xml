<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Expiry_Date__c</fullName>
    <description>The document expiry date entered by the volunteer. Blank when the parent Credential Type has Does_Not_Expire__c = true, because the form hid the expiry date input. On approval this value is copied to Credential__c.Expiry_Date__c, which may also be blank - that is the correct outcome for non-expiring credentials.</description>
    <label>Expiry Date</label>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Date</type>
</CustomField>
